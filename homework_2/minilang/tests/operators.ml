fn main() -> void {
    let a: int = 10;
    let b: int = 3;

    let sum: int = a + b;
    let diff: int = a - b;
    let prod: int = a * b;
    let quot: int = a / b;
    let mod_: int = a % b;

    print(sum);
    print(diff);
    print(prod);
    print(quot);
    print(mod_);

    let x: int = 5;
    let y: int = 5;

    if (x == y) {
        print(1);
    }
}
